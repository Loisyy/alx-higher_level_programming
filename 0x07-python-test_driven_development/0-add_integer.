#!/usr/bin/python3
"""Define the additon function of an integer.""


def add_interger(a, b=98);


	Float arguments are to be first casted to ints before the addition operation is performed.

    Raises:
        TypeError: If a or is not an integer or float.
        """
        if type(a) not in (int, float):
		raise TypeError("a must be integer")
	if type(b) not in (int , float):
		raise TypeError("b must be an interger")
	return (int(a) + int (b))
